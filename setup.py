from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='opout (or rather exploring the dataset)',
    author='le_g3',
    license='BSD-3',
)
